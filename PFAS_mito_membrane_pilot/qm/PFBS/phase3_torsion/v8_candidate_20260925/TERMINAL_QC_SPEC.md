# PFBS Phase-3 v8 terminal QC

Each point requires actual C.02 log cap 200 before SCF/MP2, exit 0, one Normal termination, optimization completed, matched nonzero SCF/MP2 counts, finite EUMP2 series, exact restraint readback, target error <=0.01 deg, 17-atom identity/order, approved 16-edge graph with no newly severe contact, known or explicitly reviewed warning class, parsed embedded archive, job-local output checkpoint, successful `formchk` on a checkpoint COPY, unchanged original checkpoint, and SHA256-bound raw input/log/chk/fchk/provenance. A finite energy alone never passes. External standalone archive is not required. Conservative contact flags produce POINT_REVIEW for scientific assessment. Local failures do not stop unrelated points; systemic failures stop new dispatch. No retries.

The Gaussian combined route is static only. Runtime gate is mandatory for any future separately authorized launch.
