# Sol High second preflight audit V6: narrow mechanical correction approved

Decision: The second preflight remains FAILED_GROMPP_PREFLIGHT. No endpoint coefficient PASS and no full MM-matrix release are claimed. The sole outer-tuple mismatch is a TPR serialization header length, so a narrowly specified comparator patch is scientifically acceptable. After independent exact-hash review of that patch and its offline tests, authorize exactly one further grompp/dump-only preflight. Until the new receipt passes, the full 1,736-case MM derivative matrix remains BLOCKED.

## Bound evidence

Failed receipt SHA-256 bc369d3aef86005d7059cd5077164b50bb8376d6e5b92366411e9b0be84cba3f; forensic JSON SHA-256 f3afe75ac8035371ac51d93679c631c7a43a4207603b993496b3bf819fe8c95f; forensic MD SHA-256 ece7400e1981c1bda0f65309b441b59f60b751333f0b2e99c72258ec7a495657. Exact runner SHA-256 c481ed84090e740a4fcd0e9d6fcb1ce72dfb1de73f39da55199e6ec011a68a31. All 84/84 selected cases generated LBFGS and physical TPRs/dumps; no mdrun occurred. BASE topology signature equals the provenance-bound formal-DP original-source reference for 15,595 lines. Accepted starts, box/pair domain, source binding and warning handling passed as recorded in the forensic report. These facts do not establish perturbation endpoint correctness, because the strict outer comparison raised first.

Across 80 BASE-versus-perturbation dump comparisons, 48 outer tuples were exact and 32 differed in exactly one pre-topology line: buffer size. It is immediately after TPR-header lambda and immediately before topology. BASE is 471877; affected perturbed values are 471913, 471917 or 471945. The first failing actual dump hashes are BASE 05cdf453b8206f453da7a1b5506e68ab14938a6f1f07bcb43a023a994b21f690 and perturbed a3be4c90bf2bea7dabce38bb1fbc5d421ec97a309896be13dce1cbc529ba2e47. All effective inputrec/nonbonded settings, coordinates, velocities, box, other header fields and footer-normalized outer lines were exact in the 80-case forensic diff.

The GROMACS v2026.3 source defines the printed header field as sh->sizeOfTprBody; when writing a TPR it assigns this from the serialized body buffer's byte length. Thus this is a file-format length, not an MM force-field or neighbor-list-buffer control. Primary source: https://gitlab.com/gromacs/gromacs/-/raw/v2026.3/src/gromacs/fileio/tpxio.cpp (write_tpx_state and pr_tpxheader). Independent read-only check of all 84 archived physical TPRs and dumps found exactly one printed integer per dump and physical.tpr byte size minus printed buffer size = 107 in every case. Counts: 471877 (52), 471913 (16), 471917 (8), 471945 (8). This is consistent with a fixed-size file header plus serialized body. The 36-byte first-failure increase exactly equals the TPR file-size increase (471984 to 472020 bytes).

## Authorized mechanical comparator rule

1. Accept exactly one decimal integer buffer size = N in the TPR header at the exact position immediately before topology. Require it to be positive, the corresponding physical TPR to exist, and the raw TPR file byte size minus N to equal 107 for this same-version double-precision build. Record raw line, integer, TPR size, difference and SHA-256 of each physical TPR and dump. If absent, duplicate, nonnumeric, misplaced, inconsistent or outside this bound format, STOP.
2. Normalize only that one header integer for BASE-versus-perturbation outer-tuple comparison. Do not normalize any other header, physical inputrec, nonbonded control, coordinates, velocities, box, topology skeleton, atom/exclusion, LJ, active instance or source/hash field. Keep the V5 context-guarded unused ld-seed and named footer normalizations unchanged.
3. Continue the canonical per-active-instance graph and complete coefficient comparison and exact perturbation endpoint assertions for every selected plus/minus h case. An outer PASS alone is not a semantic PASS. Preserve fail-stop behavior and the existing strict warnings/source/domain gates. Do not alter perturbations, amplitudes, restraints, DP binary, tolerances, starts, source set or job count.

Before another preflight, offline regressions must include: benign buffer-size variation with matching actual/fake TPR byte sizes passes; missing/duplicate/noninteger/misplaced length or mismatch with actual file size rejects; a changed neighboring header field (natoms, lambda, bTop or equivalent) rejects; changed inputrec, nonbonded physics, x/v/box or non-target active coefficient still rejects; one-sided A/B, wrong endpoint and torsion phase/multiplicity still reject. The exact patched comparator should additionally be run offline against the preserved 80 BASE-versus-perturbation dump pairs to expose any further endpoint or non-target mismatches before launching a new grompp-only attempt. Report every PASS/FAIL and bind runner/test hashes. Any further unexpected mismatch requires a new scientific review, not broad masking.

The second preflight consumed a grompp-only attempt, not a derivative or fit cycle. No new scientific variable or parameter is approved. A new grompp-only attempt is authorized only after the patched exact runner and regressions receive independent Sol review; full MM calculations remain blocked pending a complete PASS receipt.

PARAMETER_EDITED=NO. GROMACS_INVOKED_BY_REVIEWER=NO. ENDPOINT_VALIDATION=NOT_REACHED. FULL_MM_MATRIX_STATUS=BLOCKED.
