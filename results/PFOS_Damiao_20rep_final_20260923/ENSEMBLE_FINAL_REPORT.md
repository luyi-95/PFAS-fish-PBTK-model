# PFOS Damião 20-replicate ensemble final report

**ENSEMBLE_STATUS = COMPLETE**

**Scientific endpoint status: INCOMPLETE_N20**

## Reconstructed initial condition

RECONSTRUCTED INITIAL CONDITION: 5.0 nm cubic box was selected to represent the low-density random initial configuration described by Damião et al. The original paper does not report the initial box length.

## Workflow outcome

All 20 registered PFOS replicates reached a terminal workflow state with frozen MD physics, registered seeds, replicate-specific NpT mean-volume scaling, and the frozen diffusion-analysis protocol. Runtime, numerical, thermodynamic, structural, trajectory, scheduler, and provenance QA passed.

- Accepted by the frozen MSD-window rule: **7/20**
- Scientifically inconclusive under that rule: **13/20**
- Failed: **0/20**

Only 7/20 replicates met the frozen diffusion-window rule; the pre-specified n=20 mean and u95 are therefore not defined. No formal n=20 comparison with the published value is made because the frozen acceptance requirement was not met.

## Interpretation boundaries

**OBSERVATION:** The complete pre-registered workflow produced 7 accepted and 13 inconclusive diffusion estimates under the unchanged adaptive rule.

**SUPPORTED INFERENCE:** Technical execution and provenance are complete. Per-replicate accepted estimates in `DIFFUSION_RESULTS.tsv` can be reported individually.

**NOT ESTABLISHED:** When fewer than 20 replicates are accepted, the frozen protocol does not define an n=20 ensemble mean, SD, u95, or formal comparison to the published n=20 result.

**UNCERTAINTY:** The dominant observed limitation is replicate-level MSD-window eligibility under a finite 10 ns analysis interval, in addition to the documented reconstructed initial box and force-field/model assumptions.

**NEXT SCIENTIFIC STEP:** Preserve this result. Any alternative estimator, changed acceptance rule, or prospectively longer sampling design is a new protocol requiring separate approval and must not replace this frozen result.

## Provenance notes

- `continuation = yes` is a documented GROMACS 2026.3 checkpoint-restart reconstruction choice and differs from the author's SI value `continuation = no`.
- `-maxwarn 2` was used solely to accept two reviewed legacy-algorithm warnings generated by GROMACS 2026.3 for the Berendsen thermostat and Berendsen barostat. No unidentified warnings were ignored, and the published coupling algorithms were retained to preserve protocol fidelity.
- **Legacy rep02–rep04 manifest ordering repairs:** each original manifest was generated while `qa/STATUS` still contained `RUNNING`, immediately before its legitimate terminal update to `COMPLETE_INCONCLUSIVE`. Each original manifest and mismatch audit is preserved. Forensic final-state manifests were generated without changing MD physics, trajectories, analysis outputs, seeds, or scientific results.
- No trajectories, checkpoints, energy binaries, TPR/GRO files, credentials, tokens, or unrelated project files are included in this report package.
